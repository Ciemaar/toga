Fixed a bug in WinForms where resetting the background color of a Button caused it to look hollow, and added a handler to re-blend widget colors when the system switches color scheme (e.g., dark mode).
